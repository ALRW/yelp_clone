<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>New Review</title>
  </head>
  <body>
    <%= form_for [@restaurant, @review] do |f| %>
    <%= f.label :thoughts %>
    <%= f.text_field :thoughts %>
    <%= f.label :rating %>
    <%= f.select :rating, (1..5) %>
    <%= f.submit "Leave Review" %>
    <% end %>
  </body>
</html>
